#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("Hello world!\n");
    int n=0,z=0,a=0,b=0;

    if(n>0)
    {
        if (a>b)
            z=a;
    }
    else z=b;

    for(i=0, j=strlen(s)-1;i<j;i++,j--)
        c=s[i], s[i]=s[j], s[j]=c;
    return 0;
}
